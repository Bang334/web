extends ../../layout/default.pug 

block main
    h1 Thêm mới nhóm quyền
    form(
        action = `${prefixAdmin}/role/create`
        method='POST'
    )
        div(class='form-group')
            label(for="name") Nhập tiêu đề:
            input(
                type="text"
                class="form-control"
                id="name"
                name="name"
                required
            ) 
        div(class='form-group')
            label(for="des") Nhập mô tả:
            input(
                type="text"
                class="form-control"
                id="des"
                name="des"
                required
            ) 
        div(class='form-group')
            button(
                type="submit"
                class="btn btn-primary"
            ) Tạo mới